  <!-- Modal Structure -->
       
    <div class="modal-content">
      <h4>Modal Header</h4>
      <p>A bunch of text</p>
      <%= form_for(@contest, url: create_contest_path) do |f|  %>
      <% if @contest.errors.any? %>
      <div id="error_explanation">
        <h2><%= pluralize(contest.errors.count, "error") %> prohibited this contest from being saved:</h2>
  
        <ul>
        <% @contest.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
        </ul>
      </div>
    <% end %>
  
    <div class="field">
      <%= f.label :title %>
      <%= f.text_field :title, id: :contest_title %>
    </div>
  
    <div class="field">
      <%= f.label :describe %>
      <%= f.text_field :describe, id: :contest_describe %>
    </div>
  
    <div class="actions">
      <%= f.submit %>
    </div>
  <% end %>
    
    </div>
    <div class="modal-footer">
      <a href="#!" class="modal-action modal-close waves-effect waves-green btn-flat ">Agree</a>
    </div>
  