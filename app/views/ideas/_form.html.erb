<%= form_with(model: idea, local: true) do |form| %>
  <% if idea.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(idea.errors.count, "error") %> prohibited this idea from being saved:</h2>

      <ul>
      <% idea.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :title %>
    <%= form.text_field :title, id: :idea_title %>
  </div>

  <div class="field">
    <%= form.label :describe %>
    <%= form.text_field :describe, id: :idea_describe %>
  </div>

  <div class="field">
    <%= form.label :support %>
    <%= form.number_field :support, id: :idea_support %>
  </div>

  <div class="field">
    <%= form.label :local %>
    <%= form.text_field :local, id: :idea_local %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
